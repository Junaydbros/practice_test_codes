#include <stdio.h>
#include <stdlib.h>
/**
 * main - program that prints the alphabets in lowercase
 * Return: void
 */
int main(void)
{
	char ch = 'a';

	while ("ch" <= "z")
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');

	return (0);
}
