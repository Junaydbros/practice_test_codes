#include <stdio.h>

/**
 * main - program that tests the execution of a for loop
 *
 * Return: void
 */

int main(void)
{
	int a;

	for (a = 10; a >= 0; a--)
	{
		printf("%d\n", a);
	}
	printf("Blast off cos the game is on\n");

	return (0);
}
